name: gaming_library
description: "A Flutter mini-games library with leaderboards."
version: 0.0.1

environment:
  sdk: ^3.11.4
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.4
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.5.3
  google_fonts: ^6.2.1
  go_router: ^15.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # assets will be added here when logo/images are provided
